import 'package:flutter/material.dart';

class UhiNewsList extends StatelessWidget {
  const UhiNewsList({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
